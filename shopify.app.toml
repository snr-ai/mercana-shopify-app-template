# Mercana Shopify App Template
# Note: This file is a template. The CLI will overwrite it during app init.
# After init, run the setup script to restore Mercana config and deploy.

name = "Mercana"
application_url = "https://mercana.so"
embedded = false

[access_scopes]
scopes = "read_customers,read_orders,read_products"

[auth]
redirect_urls = [
  "https://api.mercana.so/auth/shopify/callback"
]

[webhooks]
api_version = "2024-01"
